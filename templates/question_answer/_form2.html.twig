{{ form_start(form) }}
{{ form_widget(form) }}
<br>
<table>
	<tr>
		<td class="">
			<button class="btn btn-primary mb-5" style="min-width:150px;">{{ button_label|default('Save') }}</button>
			{{ form_end(form) }}
		</td>
	</tr>
