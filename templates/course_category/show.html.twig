{% extends 'base.html.twig' %}

{% block title %}CourseCategory{% endblock %}

{% block body %}
    <h1>CourseCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ course_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ course_category.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ course_category.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('course_category_index') }}">back to list</a>

    <a href="{{ path('course_category_edit', {'id': course_category.id}) }}">edit</a>

    {{ include('course_category/_delete_form.html.twig') }}
{% endblock %}
