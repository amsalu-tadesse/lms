<form method="post" action="{{ path('student_course_delete', {'id': student_course.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ student_course.id) }}">
    <button class="btn btn-block btn-danger">Delete</button>
</form>
