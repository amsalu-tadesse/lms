{% extends 'base.html.twig' %}

{% block title %}Edit StudentCourse{% endblock %}
{% block breadcrumbhome %}studentcourse{% endblock %}
{% block breadcrumbcontent %}edit{% endblock %}
{% block body %}
<div class="card card-info">
    <div class="card card-header">
        <div class="row">
            <div class="col-md-6">
                <h5>StudentCourse Update</h5>
            </div>
        </div>
    </div>
    <div class="card card-body">
        {{ include('student_course/_form.html.twig', {'button_label': 'Update','class': 'form-control'}) }}
        <br>
            <div class="col-md-2">
                <a href="{{ path('student_course_index') }}" class="btn btn-primary btn-block">back to list</a>
            </div>
            <div class="col-md-2" style="padding-top: 0px;">
                {{ include('student_course/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
</div>
{% endblock %}