{% extends 'base.html.twig' %}

{% block title %}StudentCourse{% endblock %}
{% block breadcrumbhome %}studentcourse{% endblock %}
{% block breadcrumbcontent %}show{% endblock %}
{% block body %}
<div class="card">
    <div class="card-body">
        <table class="table">
            <tbody>
                                                                <tr>
                    <th>Date</th>
                    <td>{{ student_course.createdAt ? student_course.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                </tr>
                                                <tr>
                    <th>Status</th>
                    <td>{{ student_course.status }}</td>
                </tr>
                                                <tr>
                    <th>Active</th>
                    <td>{{ student_course.active ? 'Yes' : 'No' }}</td>
                </tr>
                                                <tr>
                    <th>IsAtPage</th>
                    <td>{{ student_course.isAtPage }}</td>
                </tr>
                            </tbody>
        </table>
        <br>
        <div class="row">
            <div class="col-md-2">
                <a href="{{ path('student_course_index') }}" class="btn btn-default btn-block">back to list</a>
            </div>
            {# <div class="col-md-2">
                <a href="{{ path('student_course_edit', {'id': student_course.id}) }}"
                    class="btn btn-primary btn-block">edit</a>
            </div> #}
            <div class="col-md-2">
                {{ include('student_course/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
</div>
{% endblock %}