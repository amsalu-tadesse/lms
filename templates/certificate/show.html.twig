<!DOCTYPE html>
<html lang="en">

	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>Document</title>
		<style>
			body {}
			.graph-image img {
				display: inline;
			}


			html,
			body {
				margin: 0;
				padding: 0;
			}
			.card {
				background: url("/images/background-print.jpg");
				background-size: 100% 100%;
				background-repeat: no-repeat;
				width: 900px;
				height: 500px;
				margin: 50px auto;
				font-size: 18px;
			}

            .card1{
                width: 900px;
                height: 500px;
                padding: 100px;
            }

			a {
				font-size: 14px;
			}
            .content{
                width: 530px;
                padding: 150px 300px 0px;
            }
		</style>
	</head>
	<body>
        {% if student_course %}
            <div class="card">
                <div class="content">
                    <center><h2>
                        {{student_course.student.user.firstName}}
                        {{student_course.student.user.middleName}}
                        {{student_course.student.user.LastName}}
                    </h2>
                    <hr>

                    <p>
                        has successfully completed the training on the course of</p>
                    <h4>
                        {{student_course.instructorCourse.course.name }}
                    </h4>
                    <p>
                        and awarded this qualification with
                        <b>Distinction</b>
                        on
                        <br>
                        {{ 'now'|date('M d, Y') }}</p>
                </div></center>
                <img src="data:image/png;base64,{{student_course.qrCode}}" style="margin-left: 33px;width: 88px;margin-top:-36px;">
                <p>
                    <a style="color: white;font-size: 11px; margin-left: 5px;" href="#">{{student_course.qrUrl}}</a>
                </p>
            </div>
        {% else %}
                <div class="card1">
                    <center> <h3> No Certificate Found! </h3> </center>
                </div>
        {% endif %}
		

	</body>
</html>
