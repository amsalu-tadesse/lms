<form method="post" action="{{ path('instructor_course_status_delete', {'id': instructor_course_status.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ instructor_course_status.id) }}">
    <button class="btn">Delete</button>
</form>
