{% extends 'base.html.twig' %}

{% block title %}InstructorCourseStatus{% endblock %}

{% block body %}
    <h1>InstructorCourseStatus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ instructor_course_status.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ instructor_course_status.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ instructor_course_status.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('instructor_course_status_index') }}">back to list</a>

    <a href="{{ path('instructor_course_status_edit', {'id': instructor_course_status.id}) }}">edit</a>

    {{ include('instructor_course_status/_delete_form.html.twig') }}
{% endblock %}
