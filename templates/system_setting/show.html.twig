{% extends 'base.html.twig' %}

{% block title %}{% trans %}System setting{% endtrans %}{% endblock %}

{% block body %}
    <h1>{% trans %}System setting{% endtrans %}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ system_setting.id }}</td>
            </tr>
            {# <tr>
                <th>Name</th>
                <td>{{ system_setting.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ system_setting.code }}</td>
            </tr> #}
            <tr>
                <th>{% trans %}Value{% endtrans %}</th>
                <td>{{ system_setting.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('system_setting_index') }}" class="btn btn-primary btn-sm">{% trans %}Back to list{% endtrans %}</a>

    <a href="{{ path('system_setting_edit', {'id': system_setting.id}) }}" class="btn btn-warning btn-sm">{% trans %}Edit{% endtrans %}</a>

{% endblock %}