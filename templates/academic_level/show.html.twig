{% extends 'base.html.twig' %}

{% block title %}AcademicLevel{% endblock %}
<<<<<<< HEAD
{% block breadcrumbhome %}academiclevel{% endblock %}
{% block breadcrumbcontent %}show{% endblock %}
{% block body %}
<div class="card">
    <div class="card-body">
        <table class="table">
            <tbody>
                                                                <tr>
                    <th>Name</th>
                    <td>{{ academic_level.name }}</td>
                </tr>
                                                <tr>
                    <th>Description</th>
                    <td>{{ academic_level.description }}</td>
                </tr>
                            </tbody>
        </table>
        <br>
        <div class="row">
            <div class="col-md-2">
                <a href="{{ path('academic_level_index') }}" class="btn btn-default btn-block">back to list</a>
            </div>
            <div class="col-md-2">
                <a href="{{ path('academic_level_edit', {'id': academic_level.id}) }}"
                    class="btn btn-primary btn-block">edit</a>
            </div>
            <div class="col-md-2">
                {{ include('academic_level/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
=======

{% block body %}
    <h1>AcademicLevel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ academic_level.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ academic_level.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ academic_level.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('academic_level_index') }}">back to list</a>

    <a href="{{ path('academic_level_edit', {'id': academic_level.id}) }}">edit</a>

    {{ include('academic_level/_delete_form.html.twig') }}
{% endblock %}
>>>>>>> master
