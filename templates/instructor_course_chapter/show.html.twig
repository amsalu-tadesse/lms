{% extends 'base.html.twig' %}

{% block title %}InstructorCourseChapter{% endblock %}

{% block body %}
    <h1>InstructorCourseChapter</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ instructor_course_chapter.id }}</td>
            </tr>
            <tr>
                <th>Chapter</th>
                <td>{{ instructor_course_chapter.chapter }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ instructor_course_chapter.createdAt ? instructor_course_chapter.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('instructor_course_chapter_index') }}">back to list</a>

    <a href="{{ path('instructor_course_chapter_edit', {'id': instructor_course_chapter.id}) }}">edit</a>

    {{ include('instructor_course_chapter/_delete_form.html.twig') }}
{% endblock %}
