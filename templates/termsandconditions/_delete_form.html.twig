<form method="post" action="{{ path('termsandconditions_delete', {'id': termsandcondition.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ termsandcondition.id) }}">
    <button class="btn">Delete</button>
</form>
