{% extends 'base.html.twig' %}

{% block title %}QuizQuestions{% endblock %}
{% block breadcrumbhome %}
    <a href="{{path('quiz_index',{'id':quizQustion.quiz.id})}}">
	   {% trans %}Requests{% endtrans %} 
	</a> 
{% endblock %}
{% block breadcrumbcontent %}{% trans %}Rejected{% endtrans %}{% endblock %}

{% block body %}
    <h1>QuizQuestions</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ quiz_question.id }}</td>
            </tr>
            <tr>
                <th>Question</th>
                <td>{{ quiz_question.question }}</td>
            </tr>
            <tr>
                <th>Answer</th>
                <td>{{ quiz_question.answer }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('quiz_questions_index') }}">{% trans %}Back to list{% endtrans %}</a>

    <a href="{{ path('quiz_questions_edit', {'id': quiz_question.id}) }}">edit</a>

    {{ include('quiz_questions/_delete_form.html.twig') }}
{% endblock %}
