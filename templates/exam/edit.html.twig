{% extends 'base.html.twig' %}

{% block title %}Edit Exam{% endblock %}
{% block breadcrumbhome %}
    <a href="{{ path('exam_index') }} ">
	   {% trans %}Exam{% endtrans %} 
	</a> 
{% endblock %}
{% block breadcrumbcontent %}edit{% endblock %}
{% block body %}
<div class="card card-info">
    <div class="card card-header">
        <div class="row">
            <div class="col-md-6">
                <h3>Exam Update</h3>
            </div>
        </div>
    </div>
    <div class="card card-body">
        {{ include('exam/_form.html.twig', {'button_label': 'Update','class': 'form-control'}) }}
        <br>
            <div class="col-md-2">
                <a href="{{ path('exam_index') }}" class="btn btn-primary btn-block">back to list</a>
            </div>
            <div class="col-md-2" style="padding-top: 0px;">
                {{ include('exam/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
</div>
{% endblock %}