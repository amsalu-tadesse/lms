{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>User</h1>

    <table class="table">
        <tbody>

            <tr>
                <th>First Name</th>
                <td>{{ user.firstName }}</td>
            </tr>
            <tr>
                <th>Middle Name</th>
                <td>{{ user.middleName }}</td>
            </tr>
            <tr>
                <th>Last Name</th>
                <td>{{ user.lastName }}</td>
            </tr>
       
            <tr>
                <th>User Type</th>
                <td>{{ user.userType }}</td>
            </tr>

            <tr>
                <th>email</th>
                <td>{{ user.email }}</td>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('user_index') }}" class="btn btn-primary">back to list</a>

    <a href="{{ path('user_edit', {'id': user.id}) }}" class="btn btn-info">edit</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
