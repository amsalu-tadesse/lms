<form method="post" action="{{ path('quiz_choices_delete', {'id': quiz_choice.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ quiz_choice.id) }}">
    <button class="btn">{% trans %}Delete{% endtrans %}</button>
</form>
